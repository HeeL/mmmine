<%if @product.sold?%>
  <a href="#" class="buy-now follow">Sold</a>
<%else%>
  <a href="#buy_confirm<%=@product.id%>" class="buy-now follow<%=' guest-buy-now' unless current_user%>">Buy Now</a>
<%end%>

<%= render 'shared/pop_ups/confirm_purchase' if current_user%>
