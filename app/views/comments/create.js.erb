<%if @comment.persisted?%>
  $('.comment-form:visible textarea').val('');
  $('.list-comments:visible ul').prepend('<%= j(render partial: "/products/comments/comment", locals: {comment: @comment}) %>');  
<%else%>
  show_message(@comment.errors.full_messages.first,'','error');
<%end%>
