<!DOCTYPE html>
<html xmlns:fb="http://ogp.me/ns/fb#">
<head>
  <title>mmmine</title>
  <!--[if lt IE 9]>
    <script src="/assets/html5shiv.js"></script>
    <script src="http://ie7-js.googlecode.com/svn/version/2.1(beta4)/IE9.js"></script>
  <![endif]-->
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag "http://ajax.googleapis.com/ajax/libs/jqueryui/1/themes/flick/jquery-ui.css", media: "all" %>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= render partial: 'shared/codes/ga'%>
</head>
<body>


  <%= js_msg([notice, alert])%>
  <%= js_msg([flash[:error]], 'error')%>
  <%clear_messages%>
<div class="howto"></div>
  <div class="w960">
    <header>
		  <a href="#" class="responsive-menu-toggle" data-target=".topnav-wrapper"></a>
      <a href="/" class="logo"></a>
      <%= render "shared/#{user_signed_in? ? 'user' : 'guest'}_nav" %>
			<a href="#" class="search-box-toggle" data-target=".header-search"></a>
    </header>

    <%= render 'shared/strapline' if !user_signed_in? && request.path == '/'%>

	  <%= render 'shared/right_section' if @right_section %>
	
      <%= yield %>
    

  </div>

  <%= render 'shared/pop_ups/report' %>
  <% if !current_user %>
    <%= render 'shared/pop_ups/sign_in' %>
    <%= render 'shared/pop_ups/sign_up' %>
  <%else%>
    <%= render 'shared/pop_ups/post_product' %>
  <% end %>

  <%=image_tag 'loading.gif', id: 'products_loading'%>

</body>
</html>
